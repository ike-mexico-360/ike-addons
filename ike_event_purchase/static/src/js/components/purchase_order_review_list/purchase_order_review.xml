<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">
  <t t-name="ike_event_purchase.PurchaseOrderReviewList" owl="1">
    <div class="o_purchase_order_list">
      <t t-if="state.loading">
        <div class="text-center">Loading...</div>
      </t>
      <t t-else="">
        <!-- Header KPI restricted by company settings AND supplier advanced portal permission -->
        <div t-if="state.showKpi and state.hasAdvancedPortal" class="mb-4">
          <h1 class="fw-bold display-5 mb-3">
              Order Review
          </h1>

          <div class="row g-3">

              <div class="col">
                  <div class="card kpi-card h-100">
                      <div class="card-body text-center">
                          <div class="kpi-title">Total Order Reviews</div>
                          <span class="kpi-value"><t t-out="totalOrderReviewsCount"/></span>
                      </div>
                  </div>
              </div>

              <div class="col">
                  <div class="card kpi-card h-100">
                      <div class="card-body text-center">
                          <div class="kpi-title">Subtotal</div>
                          <span class="kpi-value"><t t-out="formatCurrency(subtotalOrderReviewsAmount)"/></span>
                      </div>
                  </div>
              </div>

              <div class="col">
                  <div class="card kpi-card h-100">
                      <div class="card-body text-center">
                          <div class="kpi-title">To Consolidate</div>
                          <span class="kpi-value"><t t-out="toConsolidateCount"/></span>
                      </div>
                  </div>
              </div>

              <div class="col">
                  <div class="card kpi-card h-100">
                      <div class="card-body text-center">
                          <div class="kpi-title">Consolidated</div>
                          <span class="kpi-value"><t t-out="consolidatedCount"/></span>
                      </div>
                  </div>
              </div>

          </div>

        </div>

        <!-- Filters Bar -->
        <div class="card border-0 shadow-sm rounded-3 mb-4">
          <div class="card-body py-3">
            <div class="row g-3 align-items-end">
              <div class="col-md-5">
                <label class="form-label small text-muted fw-semibold mb-1">
                  <i class="fa fa-search me-1"/>Order Review
                </label>
                <input type="text"
                  class="form-control form-control-sm rounded-pill"
                  placeholder="Search by Order Review..."
                  t-att-value="state.filters.reference"
                  t-on-input="(ev) => this.onFilterChange('reference', ev.target.value)"/>
              </div>
              <div class="col-md-5">
                <label class="form-label small text-muted fw-semibold mb-1">
                  <i class="fa fa-calendar me-1"/>Event
                </label>
                <input type="text"
                  class="form-control form-control-sm rounded-pill"
                  placeholder="Search by event..."
                  t-att-value="state.filters.event"
                  t-on-input="(ev) => this.onFilterChange('event', ev.target.value)"/>
              </div>
              <div class="col-md-2 d-flex align-items-end">
                <button class="btn btn-sm btn-outline-secondary rounded-pill w-100"
                  title="Clear filters"
                  t-on-click="clearFilters">
                  <i class="fa fa-times"/>Clear
                </button>
              </div>
            </div>
          </div>
        </div>

        <PaginationComponent pagination="pagination">
          <table class="table table-hover table-striped align-middle mb-0" style="border-collapse: separate; border-spacing: 0;">
            <thead>
              <tr class="active">
                <th class="text-start">
                  <span class="d-none d-md-inline">Order Review</span>
                  <span class="d-block d-md-none">Ref.</span>
                </th>
                <th class="text-start">Event</th>
                <th class="text-start">Deadline</th>
                <th class="text-start">Status</th>
                <th class="text-start">Subtotal</th>
              </tr>
            </thead>
            <tbody>
              <t t-foreach="pagination.paginatedItems" t-as="order" t-key="order.id">
                <tr>
                  <td class="text-start">
                    <a t-att-href="'/my/order_review/' + order.id">
                        <t t-esc="order.name"/>
                    </a>
                  </td>
                  <td class="text-start"><t t-esc="order.x_event_id ? order.x_event_id.name : ''"/></td>
                  <td class="text-start"><t t-esc="formatDate(order.date_order)"/></td>
                  <td class="text-start">
                      <t t-esc="getStateLabel(order.state)"/>
                  </td>
                  <td class="text-start"><t t-esc="formatCurrency(order.amount_untaxed)"/></td>
                </tr>
              </t>
            </tbody>
          </table>
        </PaginationComponent>
      </t>
    </div>
  </t>
</templates>