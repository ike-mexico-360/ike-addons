<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sh_helpdesk_po_inherited_view_search" model="ir.ui.view">
        <field name="name">sh.helpdesk.po.view.search</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_filter"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='product_id']" position="after">
                <field name="x_event_id" string="Event"/>
            </xpath>

        </field>
    </record>

    <record id="sh_helpdesk_po_inherited_view_tree" model="ir.ui.view">
        <field name="name">sh.helpdesk.po.view.list</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_kpis_tree" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="x_event_id"/>
            </xpath>
        </field>
    </record>

</odoo>